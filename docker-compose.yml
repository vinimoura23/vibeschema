version: '3.8'

services:
  arch-canvas:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: arch-canvas-app
    ports:
      - "3000:80"
    restart: unless-stopped
    networks:
      - arch-network

networks:
  arch-network:
    driver: bridge
